// Shared interfaces

import { Image } from "react-native";

export interface IUser {
    first_name: string;
    last_name:  string;
    email:      string;
    phone:      string;
    img:        string;
}